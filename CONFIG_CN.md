# Syncmatica_r 配置手册 (2026-03)

**[English](CONFIG.md) | 中文**

---

Syncmatica_r 的所有运行配置都保存在单一的 `config.json` 文件中。启动时，加载器会为每个服务模块自动填充默认值。如果发现文件损坏或缺少关键配置，程序会自动重建文件。手动编辑文件后仍需重启；服务器服务配置也可以通过命令在线修改。

## 文件位置与管理

- **专用服务器和独立客户端：** 配置文件位于游戏目录下的 `config/syncmatica_r/config.json`。首次运行时会自动创建该文件夹。
- **集成服务器（单人游戏）：** 每个存档都有独立配置，路径为 `<世界文件夹>/syncmatica_r/config.json`，方便不同存档设置不同的配额和材料规则。
- **兼容旧版：** 如果找不到 `config/syncmatica_r` 但存在旧版 `config/syncmatica`，系统会一次性迁移配置。之后将只使用新版路径。
- **自动修复：** 当配置文件损坏或格式错误时，系统会用默认配置重建文件。你自定义的其他设置会被保留（但排列顺序可能变化）。

## 在线修改服务器配置

以下命令会立即更新当前服务器，并把通过校验的值写入 `config.json`：

```text
/syncmatica_r config list [模块]
/syncmatica_r config get <模块> <配置项>
/syncmatica_r config set <模块> <配置项> <值>
/syncmatica_r config reset <模块> <配置项>
```

命令界面会补全模块名和配置项。非法值会被拒绝，不会静默调整。`reset` 每次只恢复一个配置项。
这些命令要求 `syncmatica_r.config` 权限，默认回退到原版权限等级 2。客户端更新检查和界面偏好不在此范围内。

## 配置结构说明

配置文件采用模块化设计。服务器会用到下面全部模块，而客户端只需要关注 `debug` 模块。此外，根对象上还可以配置两个与 GitHub 更新检查相关的开关。

```json
{
  "checkupdate": true,
  "check_pre_release": false,
  "quota": {
    "enabled": false,
    "limit": 40000000
  },
  "materials": {
    "enabled": true,
    "scan_interval": 200,
    "scan_blocks_per_tick": 2048,
    "include_container_contents": false,
    "allow_owner_stocking_area_management": true,
    "max_schematic_megabytes": 64,
    "max_schematic_blocks": 8000000,
    "max_stocking_area_blocks": 1000000
  },
  "build": {
    "enabled": true,
    "completion_enabled": true,
    "scan_blocks_per_tick": 4096,
    "scan_interval": 1200,
    "full_rescan_interval": 36000
  },
  "debug": {
    "doPackageLogging": false
  }
}
```

如果你是客户端用户，可以放心删除 `quota`、`materials` 和 `build` 模块。将来如果开启服务器，系统会自动重新生成这些配置；根级的更新检查开关只在客户端生效，服务器会忽略它们。

## `quota` — 上传流量控制（服务器）

| 配置项 | 默认值   | 取值范围      | 说明 |
|--------|----------|---------------|------|
| enabled | `false`  | `true`/`false`| 开启后，系统会统计服务端从每个玩家接收的原理图上传流量 |
| limit   | `40000000`| ≥ 0 (字节)    | 单个玩家在服务器运行期间允许上传的原理图总字节数 |

使用说明：
- 流量统计基于玩家身份，每个已接收的数据块都会计入，包括最终失败的传输；重新连接不会清零，服务器重启后才会清零
- 如果不需要限制流量，请保持 `enabled` 为 `false`，这样可以减少系统开销
- 即使关闭流量统计，单文件大小、数据包大小、并发交换数和交换超时仍受硬限制

## `materials` — 材料清单追踪（服务器）

此功能仅限服务器使用，用于计算和同步建筑图纸所需的材料数量。

| 配置项                   | 默认值  | 最低限制      | 作用 |
|--------------------------|---------|---------------|------|
| enabled                  | `true`  | —             | 总开关，控制材料统计功能是否启用 |
| scan_interval            | `200`   | `20` 游戏刻   | 自动扫描材料储备区的频率 |
| scan_blocks_per_tick     | `2048`  | `64`–`65,536` 个方块 | 增量扫描共享的每刻工作预算 |
| include_container_contents | `false` | —             | 是否统计箱子、潜影盒等容器内的物品 |
| allow_owner_stocking_area_management | `true` | — | 是否允许投影所有者通过命令或 GUI 设置自己的备货区；关闭后两条路径都要求 `syncmatica_r.manage` |
| max_schematic_megabytes  | `64`    | `1`–`64` MB   | 压缩文件传输和解压后 NBT 分配的大小上限 |
| max_schematic_blocks     | `8000000`| `1,000,000`–`64,000,000` | 图纸解码方块数量上限 |
| max_stocking_area_blocks | `1000000`| `1,024`–`64,000,000` | 备货区允许的最大体积 |

实用建议：

- 如果服务器扫描时出现卡顿，可以适当降低 `scan_blocks_per_tick` 并增加 `scan_interval`
- 图纸解析在有资源上限的后台线程执行，嵌套容器最多遍历 10 层，结果回到服务端线程后才会生效
- 备货区命令只安排增量扫描，不再在命令执行期间一次扫描完整区域
- `allow_owner_stocking_area_management` 只影响投影专属备货区；默认备货区始终要求
  `syncmatica_r.manage`
- 命令修改会立即生效。材料提取限制或容器内容开关变化后会重新提取所有共享项目；修改备货区上限会重建扫描

## `build` — 建造管理（服务器）

此模块仅限服务器使用。关掉它会把 `BUILD_MANAGEMENT` 从广播的功能集里撤掉，所有客户端都看不到区域认领。

建造管理自己读蓝图文件，不复用材料提取的结果，所以两个功能互不依赖：关掉 `materials` 不影响区域认领和完成度，反过来
也一样。代价是服务器启动时每个放置多解码一次。

| 配置项               | 默认值  | 最低限制              | 作用 |
|----------------------|---------|-----------------------|------|
| enabled              | `true`  | —                     | 总开关，控制区域认领及以下全部功能 |
| completion_enabled   | `true`  | —                     | 是否测量每个区域建了多少。关掉后认领照常可用 |
| scan_blocks_per_tick | `4096`  | `64`–`65,536` 个方块  | 完成度扫描每游戏刻的工作预算 |
| scan_interval        | `1200`  | `100` 游戏刻          | 一个放置里还有没数过的区块列时，隔多久回去重试一次 |
| full_rescan_interval | `36000` | `1200` 游戏刻，或 `0` | 隔多久把所有区块列从头重数一遍。填 `0` 关掉这轮兜底 |

实用建议：

- 扫描由方块变化驱动。服务器会把每次方块变化告诉建造管理，所以没人动过的放置根本不扫，正在施工的放置也只重数发生变化
  的那些区块列。放下一个方块后一两个游戏刻内进度就会更新，闲置的蓝图不管多大都不花代价
- 同一时刻只扫描一个放置。大蓝图的代价是扫描时间长，而不是服务器卡顿
- 完成度按区块列统计并保留。未加载的区块里不可能有人施工，所以视野外的区块列保留上一次数出来的值。区域比玩家能加载的
  范围大得多也照样能测出来，只是分成多次、随着大家走到哪测到哪。从来没有人加载过的区块列按未建造计算，
  `scan_interval` 就是隔多久回去看看它们能不能读到了
- 有些写入不经过游戏本身，比如批量编辑工具直接改区块 section，这类改动不会被上报，会留下过期的统计数。
  `full_rescan_interval` 就是兜底重数这一轮。服务器上没有这类工具时可以调长；填 `0` 完全关掉，之后只能靠
  `rescanBuild` 手动恢复
- 每个区域只扫蓝图里真正有内容的层，每个放置解码后的蓝图数据也会留到下一轮复用，不再每轮重读文件。这两项都不改变
  统计口径
- 统计数据存在它测量的那个世界里，位于 `<存档>/syncmatica_r/build_scan/`，不跟放置数据放在一起。所以还原备份、回档时，
  对应的统计数据会跟着一起回去；世界里的数据说了算，放置文件记的那份不算
- 关服后改地图、用别的工具直接写 region 文件，仍然会绕过统计。这时用 `/syncmatica_r <项目名> rescanBuild`
  把统计数据全部丢掉，按世界里实际的样子重新测
- 完成度只比较方块种类，不比较完整方块状态。整片楼梯朝向都装反了也算完成 —— 和材料清单的统计口径一致
- 认领按区域名记录，重新分享、重新提取、重启服务器都不会丢。区域从蓝图里消失时，它的认领也一起消失
- 有两个开关属于玩家而不是服主，所以不在这里配置。两个都能在 MaLiLib 客户端设置和区域列表顶部修改，
  配置保存在 `config/syncmatica_r/client.json`：
  - 越界建造提醒，对应 `General.warnOnForeignPlacement`，默认开启
  - Litematica 子区域可见性跟随认领，对应 `General.followClaims`，默认关闭。打开后，认领一个区域会打开对应子区域，
    取消认领或把它建完都会关掉它；别人认领的区域和无人认领的区域完全不动，保持玩家自己设的状态
- 修改这些配置必须重启服务器才能生效

## 客户端上限提示

上面这些上限拦下操作时，服务器会告诉客户端具体是哪一条限制，而不是静默失败。支持 `LIMIT_REPORT` 特性的客户端还会
额外收到实际值与上限值，格式与语言无关，例如 `96.0 MB > 64.0 MB`；旧客户端仍然只显示原来的提示文本，通信格式保持
不变。

| 场景 | 提示位置 | 相关配置 |
|------|----------|----------|
| 分享的投影超过允许的传输大小 | 分享方客户端的错误提示 | `materials.max_schematic_megabytes` |
| 分享的投影耗尽玩家上传配额 | 分享方客户端的错误提示 | `quota.limit` |
| 服务器上已存的投影大于当前上限，无法下发 | 下载方客户端的错误提示，同时立即取消下载而不是等超时 | `materials.max_schematic_megabytes` |
| 材料列表无法生成 | 打开材料列表界面时的原因提示、材料按钮的悬停文本，以及发给放置所有者的一次性消息 | `materials.enabled`、`materials.max_schematic_megabytes`、`materials.max_schematic_blocks` |

最常见的原因是把 `max_schematic_megabytes` 或 `max_schematic_blocks` 调到了已分享投影的体积以下：这些放置的元数据还
在，但下载和材料列表都会失效，现在每个受影响的客户端都能看到是哪条限制拦下的。

## `debug` — 调试模式（客户端和服务器）

| 配置项          | 默认值 | 说明 |
|-----------------|--------|------|
| doPackageLogging | `false`| 开启后会在日志中记录所有数据包收发详情 |

注意事项：
- **生产环境请务必保持关闭**，否则会生成大量日志并可能泄露敏感信息
- 仅在排查网络问题时临时开启

## 客户端更新开关：`checkupdate` 与 `check_pre_release`

这两个配置项位于配置文件的根对象，仅在客户端生效。

| 配置项            | 默认值  | 说明 |
|-------------------|---------|------|
| `checkupdate`     | `true`  | 总开关。设为 `false` 时，客户端不会访问 GitHub，也不会弹出任何更新提示。 |
| `check_pre_release` | `false` | 预发布检查开关。为 `false` 时，正式版客户端只会把更高版本的正式版视为更新；为 `true` 时，正式版客户端也会把更高版本号的预发布版本当作可选更新。预发布客户端始终可以看到更高版本的预发布和正式版更新。 |


## 常见问题

- **配置被重置？** 可能是 JSON 格式错误。请在重建的文件上重新修改。
- **设置自动恢复？** 系统会自动补全缺失的配置项。要禁用某个功能，请将其值改为 `false` 而不是删除整行。
- **材料界面仍然显示？** 请确认修改的是服务器配置文件（不是客户端），并且已经重启服务器使配置生效。

## 网络权限

- `syncmatica_r.share`：上传新放置；权限插件未处理时默认允许。
- `syncmatica_r.claim`：认领已有材料需求；权限插件未处理时默认允许。
- `syncmatica_r.build.claim`：认领共享蓝图的子区域；权限插件未处理时默认允许。和 `syncmatica_r.claim` 分开，
  方便把「收材料」和「建某一块」分给不同的人。
- `syncmatica_r.manage`：修改或删除其他玩家的放置、管理任意投影的备货区及默认备货区；默认回退到权限等级 2。
- 放置所有者始终可以修改或删除自己的放置。
