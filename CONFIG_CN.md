# Syncmatica_r 配置手册 (2025-11)

**[English](CONFIG.md) | 中文**

---

Syncmatica_r 将所有运行时配置保存在单个 `config.json` 文件中。加载器在启动时会合并每个服务部分的默认值，并在需要插入缺失键或从无效 JSON 恢复时重写文件。因此，请始终在游戏/服务器停止时编辑文件；更改仅在下次启动时读取。

## 位置与生命周期

- **独立服务器与客户端：** 游戏目录下的 `config/syncmatica_r/config.json`。如果文件夹不存在，会自动创建。
- **集成（单人）服务器：** `<世界文件夹>/syncmatica_r/config.json`，这样每个存档可以拥有自己的配额和材料策略。
- **旧版回退：** 如果 `config/syncmatica_r` 不存在但 `config/syncmatica` 存在，加载器会一次性读取旧版文件。一旦创建现代文件夹，将只使用 `syncmatica_r`。
- **错误恢复：** 当 JSON 无法解析或缺少某个部分时，Syncmatica_r 会用最后已知的良好配置加上默认值重写整个文件。未知键会被保留，但顺序可能会改变。

## JSON 结构概览

根对象是一个扁平的部分集合。服务器使用下面显示的所有三个块，而客户端只关心 `debug`。

```json
{
  "quota": {
    "enabled": false,
    "limit": 40000000
  },
  "materials": {
    "enabled": true,
    "scan_interval": 200,
    "scan_blocks_per_tick": 2048,
    "include_container_contents": false,
    "max_schematic_megabytes": 64,
    "max_schematic_blocks": 8000000
  },
  "debug": {
    "doPackageLogging": false
  }
}
```

客户端可以安全删除 `quota` 和 `materials` 部分；当游戏稍后作为服务器运行时，加载器会重新创建它们。

## `quota` — 服务器上传控制

| 键      | 默认值      | 范围              | 含义                                                                 |
|---------|-----------|------------------|----------------------------------------------------------------------|
| enabled | `false`   | `true` / `false` | 为 `true` 时，每个上传/下载交换都会按玩家跟踪。                       |
| limit   | `40000000`| ≥ 0（字节）       | 玩家在当前服务器运行期间可以传输的总字节数。                           |

- 限制适用于每个 `ExchangeTarget`（玩家身份）。计数器在服务器关闭时重置，因为服务在 `shutdown()` 期间清除其内存中的 `progress` 映射。
- 当不需要带宽限制时，将 `enabled` 设置为 `false`；代码会短路并且永不记录配额数据。

## `materials` — 服务器材料跟踪

此块仅在 MaterialService 存在时（独立或集成服务器）使用。禁用该块会从通告的功能集中移除材料进度/声明。

| 键                          | 默认值    | 强制最小值       | 用途                                                                 |
|----------------------------|----------|-----------------|----------------------------------------------------------------------|
| enabled                    | `true`   | —               | 材料聚合和同步的主开关。                                               |
| scan_interval              | `200`    | `20` 刻         | 默认储料区在空闲时重新扫描的频率。                                     |
| scan_blocks_per_tick       | `2048`   | `64` 方块       | 每次活动扫描的工作预算。较高的值可以更快完成长投影，但每刻占用更多 CPU。 |
| include_container_contents | `false`  | —               | 为 `true` 时，投影内的箱子/潜影盒会将其库存计入材料数量。               |
| max_schematic_megabytes    | `64`     | `1` MB          | 大于此值的 Litematic 文件在需求提取期间会被跳过，以避免 I/O 滥用。      |
| max_schematic_blocks       | `8000000`| `1,000,000`     | 传递给提取器的上限，以避免解码不合理的大方块体积。                      |

操作说明：

- `scan_blocks_per_tick` 和 `scan_interval` 协同工作。如果服务器卡顿，降低它们；如果扫描时间过长，提高它们。
- 两个 `max_schematic_*` 保护措施可防止玩家强制进行数 GB 的解析。触发时，服务器只会记录警告并跳过该放置的需求提取。
- 在服务启动后更改这些设置需要完全重启服务器。

## `debug` — 数据包日志（客户端 + 服务器）

| 键               | 默认值   | 含义                                                                              |
|-----------------|---------|-----------------------------------------------------------------------------------|
| doPackageLogging| `false` | 启用时，每个 Syncmatica_r 数据包的发送/接收都会通过 Log4j 在 `INFO` 级别记录。     |

在生产服务器上保持此开关关闭；它很吵闹并且在纯文本日志中暴露数据包元数据。仅在诊断协议问题时切换它。

## 故障排除

- 启动后文件重置？JSON 语法无效。在重新生成的文件上重新应用你的编辑。
- 选项消失？加载器总是添加缺失的键和默认值，因此仅删除的编辑将在下次运行时恢复。保留键并更改其值。
- 将 `enabled` 设置为 `false` 后材料 UI 仍然可见？确保你编辑的是服务器的配置（不是客户端）并重启服务器，以便新标志传播到通告的功能集中。
